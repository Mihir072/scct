spring:
  profiles:
    active: prod
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    baseline-on-migrate: true

jwt:
  secret: ${JWT_SECRET:dGhpcy1pcy1hLXN1cGVyLXNlY3JldC1rZXktZm9yLWp3dC10b2tlbi1nZW5lcmF0aW9uLXNjY3QtYmFja2VuZA==}
  expiration: 28800000 # 8 hours in milliseconds (8 * 60 * 60 * 1000)

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:https://scct-mu.vercel.app,http://localhost:3000}

mail:
  from-email: ${MAIL_FROM:supportscct@gmail.com}

server:
  port: ${PORT:8080}
