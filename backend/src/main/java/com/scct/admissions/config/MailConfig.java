package com.scct.admissions.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MailConfig {
    // Starter-mail autoconfiguration is configured via application.yml/local.yml/prod.yml
}
